<Window x:Class="HM.Presentation.WPF.Views.Clients.Dialogs.ClientDetailsDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        d:DesignHeight="400" d:DesignWidth="500"
        Width="400"
        SizeToContent="Height"
        ResizeMode="NoResize"
        Title="Client Details"
        Background="White">
    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Padding" Value="15,6" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4" />
                </Style>
            </Style.Resources>
        </Style>

        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,10,0,2" />
            <Setter Property="Foreground" Value="#555555" />
        </Style>

        <Style TargetType="TextBlock" x:Key="ValueStyle">
            <Setter Property="Margin" Value="0,0,0,10" />
            <Setter Property="Foreground" Value="#333333" />
            <Setter Property="FontSize" Value="14" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Client Details" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="First Name" Style="{StaticResource LabelStyle}" />
                <TextBlock Text="{Binding User.FirstName}" Style="{StaticResource ValueStyle}" />

                <TextBlock Text="Last Name" Style="{StaticResource LabelStyle}" />
                <TextBlock Text="{Binding User.LastName}" Style="{StaticResource ValueStyle}" />

                <TextBlock Text="Email" Style="{StaticResource LabelStyle}" />
                <TextBlock Text="{Binding User.Email}" Style="{StaticResource ValueStyle}" />

                <TextBlock Text="Phone Number" Style="{StaticResource LabelStyle}" />
                <TextBlock Text="{Binding User.PhoneNumber}" Style="{StaticResource ValueStyle}" />


            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Create Booking" Command="{Binding CreateBookingCommand}"
                    Style="{StaticResource PrimaryButtonStyle}" Background="#2E7D32" />
            <Button Content="Close" Command="{Binding CloseCommand}" Style="{StaticResource PrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</Window>